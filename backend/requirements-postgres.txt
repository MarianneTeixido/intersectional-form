# Dependencias extra para el modo PostgreSQL (servidor).
# En local no hacen falta: sin DATABASE_URL el almacén es el JSON.
#
#   pip install -r requirements.txt -r requirements-postgres.txt
psycopg[binary]>=3.1
psycopg_pool>=3.2
